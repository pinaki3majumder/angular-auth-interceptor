import { HttpErrorHandler } from './http-error-handler.util';

describe('HttpErrorHandler', () => {
  it('should create an instance', () => {
    expect(new HttpErrorHandler()).toBeTruthy();
  });
});
